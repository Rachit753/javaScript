//TypeCoersion :- It is a concept in js in which while performing operation between two dt js convert them into one for performaing operations.

let a = "5" + 1; // + sign have two option add and concatination where js see string it will concatenate.
console.log(a);

let b = "5" - 1; // - sign only have one option which is substract so it will substract.
console.log(b)

//Also JAVASCRIPT is Dynamically typed which means we can convert one dt into another while performing operations.

//TRUTHY vs FALSY VALUES means every value in js have only two nature true or false.

// Falsy :- 0, null, NAN, undefined, false, "",document.all ------rest all truthy.
!!0 // for checking nature.